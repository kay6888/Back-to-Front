package com.backtorfront.app.ui.theme

import androidx.compose.ui.graphics.Color

// ── Dark "Hacker" Palette ──────────────────────────────────────────────────

// Backgrounds — deep purple / near-black
val BackgroundDeepPurple = Color(0xFF0D0A1E)
val SurfaceDarkPurple    = Color(0xFF130F2B)
val SurfaceVariantPurple = Color(0xFF1E1840)

// Primary accent — baby blue
val BabyBlue             = Color(0xFF89CFF0)
val BabyBlueDim          = Color(0xFF5FA8CC)
val BabyBlueLight        = Color(0xFFB8E4FF)

// Secondary / tertiary
val NeonCyan             = Color(0xFF00E5FF)
val PurpleGlow           = Color(0xFF9C6ADE)

// Text
val TextPrimary          = Color(0xFF89CFF0)   // baby blue
val TextSecondary        = Color(0xFF6BAAC8)
val TextMuted            = Color(0xFF3D6E8A)

// Error
val ErrorRed             = Color(0xFFFF6B6B)

// On-colors (icons / text drawn on top of these surfaces)
val OnPrimary            = Color(0xFF0D0A1E)
val OnBackground         = BabyBlue
val OnSurface            = BabyBlue
val OnError              = Color(0xFF1A0000)
